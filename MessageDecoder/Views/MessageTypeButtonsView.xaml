<UserControl x:Class="MessageDecoder.Views.MessageTypeButtonsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MessageDecoder.Views"
             xmlns:viewModel="clr-namespace:MessageDecoder.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

   <UserControl.DataContext>
      <viewModel:MessageTypeTreeViewModel/>
   </UserControl.DataContext>
   
   <Grid>

      <StackPanel>

         <Button Content="Rename"
                 Command="{Binding RenameCommand}"
                 CommandParameter="Hello"
                 VerticalAlignment="Top"
                 HorizontalAlignment="Center"
                 Width="100"/>

         <Button Content="Delete"
                 Command="{Binding DeleteCommand}"
                 VerticalAlignment="Center"
                 HorizontalAlignment="Center"
                 Width="100"/>


         <Button Content="New Subtype"
                 VerticalAlignment="Bottom"
                 HorizontalAlignment="Center"
                 Width="100"/>
         <!--
                 Command="{Binding NewCommand}"-->

      </StackPanel>

    </Grid>
</UserControl>
